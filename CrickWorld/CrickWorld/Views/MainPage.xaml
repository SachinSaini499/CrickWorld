<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:local="clr-namespace:CrickWorld;"
              xmlns:ViewModels="clr-namespace:CrickWorld.ViewModels;"
             x:Class="CrickWorld.Views.MainPage"
            
             >
    <ContentPage.BindingContext>
        <ViewModels:UserViewModel/>
    </ContentPage.BindingContext>
    
    <StackLayout>
        <Button Text="Sign UP" Clicked="Button_OnClicked" ></Button>
    <ListView ItemsSource="{Binding userList}">
     <ListView.ItemTemplate>
         <DataTemplate>
            <ViewCell>
                    <Label Text="{UserName}"></Label>
                    <Label Text="{PhoneNumber}"></Label>

                </ViewCell>
                 
                

            </DataTemplate>
     </ListView.ItemTemplate>
    </ListView>
    </StackLayout>
</ContentPage>